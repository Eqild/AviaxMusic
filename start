#!/bin/bash
cd "$(dirname "$0")" || exit 1
export PYTHONPATH="${PYTHONPATH}:$(pwd)"

# Start the bot in the background
python3 -m anony &

# Start the lightweight web server in the foreground so the container binds to $PORT
python3 web.py
